{
  "name": "alog-api",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "start": "wrangler dev",
    "test": "node --test test/worker.test.js"
  },
  "devDependencies": {
    "wrangler": "^4.14.4"
  }
}